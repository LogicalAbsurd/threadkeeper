# Handoff: Vault + Threadkeeper Project (Resume Point)

## Who I am

Chris (Anaximander Immanuel Aletheia online / @LogicalAbsurd on GitHub). Late 30s, Buffalo NY, server/bartender at PF Chang's. Bipolar + ADHD, well-managed. Fedora Linux, HP OMEN desktop, 12th gen i7, RTX 3080 12GB VRAM, 16GB RAM, 635GB encrypted home.

Hobbyist dev, novice-level coding. Vibe-coding mode — I copy/paste commands you give me, don't read code line-by-line. Push back on me when warranted, including on my decisions. Don't psychoanalyze unprompted. I have genuine mystical/Christian experiences in my background; treat them with respect, not skepticism, but don't validate things I'm clearly processing. Match my prose energy — purple when I'm in that mode, plain when I'm tired.

## How we work

- Claude.ai (you) is the architect; Claude Code in terminal is the executor for Threadkeeper work
- One command at a time, brief explanations of what each does
- ask_user_input_v0 for multi-option decisions
- Don't try to manage my sleep or push me away from work — I'll tell you when I'm done

## Project state (as of handoff)

### Vault — DONE
Location: `~/Documents/General/` (Obsidian vault). ~3116 .md files total.

Complete migrations:
- Notesnook (~200 notes)
- .docx/.odt writings (71 files)
- Anthropic export (65 conversations) → AI Chats/Anthropic/
- Gemini bulk export via Threadkeeper (199 conversations) → AI Chats/Gemini/
- OpenAI official export (629 conversations) → AI Chats/OpenAI/
- Reddit (50 comments + 1 post) → Social/Reddit/
- YouTube (605 comments → 481 grouped files) → Social/YouTube/
- Threads (102 posts + 820 replies + 22 archived) → Social/Threads/
- Instagram (70 comments) → Social/Instagram/
- **X/Twitter (518 posts, grouped into conversational threads)** → Social/X/Threads/

Parser scripts at `~/scripts/export_to_markdown/`:
parse_anthropic.py, parse_openai.py, parse_reddit.py, parse_youtube.py, parse_threads.py, **parse_x.py**, convert_to_md.sh

Source zips backed up at `~/backups/{Anthropic,Google,OpenAI,Reddit,Meta,X}/`

### Obsidian Sync — RESOLVED
- Earlier had a sync scare: ~600 files showed up in "Deleted Files" panel after adding phone as second device
- Resolved (with ChatGPT's help) by: pausing sync, verifying desktop intact, destroying the corrupted remote, wiping phone-local vault, rebuilding sync clean from desktop
- New architecture: Desktop = authoritative source-of-truth → Remote = clean mirror → Phone = downstream consumer
- No more bidirectional ambiguity. Should be stable.

### Disk — CLEAN
- Was at 89% (danger zone). Now at 68% (204 GB free).
- Cleaned trash (7.6 GB), .cache (40 GB), 14 dormant local LLM model .gguf files (~95 GB), 2 dormant venvs (thornyrose, omega — ~17 GB)
- **Keeper models** at `~/dev/projects/ai/anaximander/models/`: ARC-Base-8B (intellectually rigorous), Hermes-4-14B-BF16-abliterated (current favorite, runs at -ngl 24 with CPU offload), HERETIC (good context with annoying mind-mining). Plus quirks_of_each_model.txt and README.md — preserve those, they're qualitative research notes.

### Threadkeeper extension — Phase 5 VERIFIED IN PRODUCTION (2026-05-26)
Repo: `~/dev/projects/experiments/chat-archiver` → https://github.com/LogicalAbsurd/chat-archiver (private)
License: AGPL-3.0, Manifest V3, Firefox-first, vanilla JS, no build step.

Phase 5 ChatGPT scraper tested live against ~641 real conversations (22 active + 619 archived):
- Single-chat export, bulk export, and selective export all working
- Search/filter UI fixed (CSS bug where display:flex was overriding [hidden] attribute)
- Archive support added via opt-in checkbox; queries /backend-api/conversations?is_archived=true as second pass and concatenates with active list
- Bug fixed: NaN/null create_time values now handled gracefully (was throwing "invalid date" mid-listing)
- Tested up to 1,636-message conversations without parser issues (~7 seconds per large chat via API)
- No rate-limiting encountered at small batch sizes; 500ms delay between conversations holds
- DECISION: full 641 run intentionally NOT executed — would duplicate the 629-chat OpenAI official export already in vault. Threadkeeper's role going forward is delta capture (new chats post-export), not bulk historical archival.

Phase status:
- Phase 1-4 ✅ scaffold, popup UI, Gemini bulk export (battle-tested, 199 conversations exported successfully)
- Phase 5 ✅ VERIFIED — ChatGPT internal API scraper (adapted from Pionxzh's MIT-licensed chatgpt-exporter, credited in README + ACKNOWLEDGMENTS.md). Auth via /api/auth/session cookie endpoint (content scripts can't access __remixContext globals).
- Phase 6 (Claude.ai support) — DEFERRED, clean Anthropic export already covers it
- Phase 7 (polish + store submission) — future work

Architecture (encoded in repo's CLAUDE.md):
- All scrapers implement `listConversations()`, `loadConversation(id)`, `parseMessages()` triad
- CONTENT_READY handshake with chatId matching for SPA navigation safety
- Three-technique lazy-load loop for Gemini (scrollTop=0 / overscroll / scrollIntoView({block: 'end'}))
- Background owns navigation via tabs.update(); content scripts never navigate
- ChatGPT uses API approach (no DOM scraping, no lazy-load), so much simpler

## Next-stage roadmap (in rough priority order)

1. **Deterministic auto-tagging** of the 3116-file vault. (Threadkeeper Phase 5 complete; future Threadkeeper work deferred to Phase 6/7.)
2. **Semantic linking** between related files.
3. **Embeddings/RAG integration** — possibly local via Ollama or via API.
4. **Dataview / query infrastructure** in Obsidian.
5. **Graph emergence** — let the structure reveal itself once tags/links exist.
6. **AI-assisted vault ontology development** — collaborative organization.
7. **Threadkeeper Phase 7** polish + store submission.

## Operational principle (established this week)

**"Structure before enchantment"** — meaning:
- Backup first, always
- Dry-run any transformation before applying
- Deterministic organization before AI mutation
- Preserve raw source archives alongside parsed markdown outputs

### Where I want to start next

Phase 5 wrapped 2026-05-26 late evening. Resume points:
- Deterministic auto-tagging of vault (next priority)
- Optional: run Threadkeeper to capture the ~12 ChatGPT chats that postdate the OpenAI export (the active-sidebar 22 minus duplicates already in vault)
- Threadkeeper Phase 6 (Claude.ai scraper) — low priority, Anthropic export already covers it
- Threadkeeper Phase 7 (polish + store submission) — future work
